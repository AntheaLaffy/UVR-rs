# 任务与验收

状态日期：2026-09-09。当前阶段：P1 先验方案讨论；仓库初始化完成，推理实现尚未开始。

## 已明确的需求

| 任务 | 指定权重 | 目标输出 |
| --- | --- | --- |
| 分离伴奏 | `model_bs_roformer_ep_368_sdr_12.9628.ckpt`（viperx 1296） | 人声、伴奏 |
| 分离和声 | `5_HP-Karaoke-UVR.pth`、`6_HP-Karaoke-UVR.pth` | 主唱与和声侧输出，具体映射待参考验证 |
| 去混响与回声 | `UVR-DeEcho-DeReverb.pth` | 干声与残余混响／回声侧输出 |

推理优先纯 Rust，GUI 例外。GUI 按既有目录说明采用 Tauri 与 TypeScript。首版不实现 DeepSeek Harness、MCP 或 AI 问答；不扩展到完整 UVR 模型目录。

## 阶段

### P0：初始化（已完成）

- [x] 初始化 Rust core、CLI、Tauri 宿主和 pnpm GUI workspace。
- [x] 固定参考仓库版本，避免嵌套仓库意外丢失或混入源码。
- [x] 建立任务、先验、后验与性能文档。
- [x] 完成构建检查并提交初始化基线（本文件随该基线提交；验证记录见后验文档）。

### P1：方案讨论（初始化后）

- [x] 建立 [算法与推理架构的先验草案](inference-priors.md)，与后验验证结论分开记录。
- [ ] 确认“纯 Rust”对底层 GPU 驱动、计算库 FFI、离线权重转换工具的边界。
- [ ] 确认首要操作系统、CPU／GPU 硬件、内存预算、离线部署要求。
- [ ] 选择推理框架、算子实现与权重存储方案，记录选择依据。
- [ ] 固定客户实际使用的 UVR／推理程序版本、参数和参考音频。
- [ ] 确认 CLI／GUI 的输入输出与处理链，以及验收误差和性能目标。

### P2：正确性基线

- [ ] 验证四套权重的来源、校验和、配置对应关系与分发条件。
- [ ] 建立解码、重采样、STFT／ISTFT、分块与分轨重建基线。
- [ ] 分别验证 BS-RoFormer、旧版 VR、含 LSTM 的新版 VR；建议顺序见先验草案 H07，按讨论和实验结果修订。
- [ ] 保存可复现的中间张量与参考输出，覆盖静音、短音频、非整数块长度和声道处理。

### P3：性能与产品接入

- [ ] 依照性能协议测量端到端及分阶段开销，先定位瓶颈再优化。
- [ ] 接入 GUI 任务进度、取消和错误展示；保持推理在 core。
- [ ] 用客户素材比较去和声／去混响顺序及单模型参数，确认可接受预设。

## 首版完成条件

四套指定权重通过约定的正确性与音频质量验收；CLI 和 GUI 可执行约定的分离任务；输出声道、长度、增益与分轨语义明确；达到 P1 约定的硬件及性能目标。每项结论均有可复现记录，未实现能力不能显示为可用。
