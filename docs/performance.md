# 性能协议

状态：2026-09-09，尚无推理实现和测量结果。目标硬件、耗时预算、内存上限及质量阈值待方案讨论确定。本文件定义如何测量，不把客户经验或初始化构建时间当作推理基准。

## 测量对象

分别测量四套权重及最终处理链。先验证数值和音质，再比较性能；同一模型比较需固定输入、分块、重叠、batch、精度、TTA、aggressiveness、高频处理和输出编码。不得把关闭后处理带来的速度变化记为同质量加速。

| 指标 | 定义 |
| --- | --- |
| 冷启动耗时 | 新进程启动到完成首个任务，包含模型加载与后端初始化；注明文件系统缓存是否受控 |
| 热运行耗时 | 同一进程复用已加载模型的完整任务时间 |
| RTF | 处理墙钟时间 / 输入音频时长，越低越快；明确计时是否包含 I/O |
| 处理倍速 | 输入音频时长 / 处理墙钟时间，即 1 / RTF |
| 分阶段耗时 | 解码、重采样、STFT、网络、ISTFT／重建、编码分别记录 |
| 峰值资源 | 进程 RSS 与可获取的设备显存分别记录，注明采样方式与间隔 |
| 交互响应 | 首次进度、取消请求到停止的延迟；GUI 接入后测量 |
| 质量 | 对参考结果的波形／频谱误差与残差，必要时有真值数据上的 SDR／SI-SDR 和客户听评 |

GPU 计时必须同步或使用设备计时事件，避免只测到异步提交。统一内存设备注明内存指标是否重复计算。不要通过独立归一化每条分轨掩盖增益误差。

## 输入矩阵

- 静音、脉冲、合成音：数值稳定性、时间对齐、窗和重采样验证。
- 单声道与双声道、44.1 kHz 与需要重采样的输入。
- 短于单块、恰好单块、非整数块、普通整曲与长音频。
- 经允许使用的干声、和声、强混响、明显 echo 素材及完整混音。

输入清单记录校验和、采样率、声道、采样数、格式和用途。客户素材保存在本地，版本库只提交允许公开的清单和汇总。

## 执行方法

1. 固定 Git 提交、Cargo.lock、权重及配置校验和、参考程序版本和全部推理参数。
2. 使用 release 构建，记录编译特性、RUSTFLAGS、CPU／GPU、内存、驱动、系统、电源模式和线程数。
3. 单独报告冷启动；热运行先预热，再至少执行 5 次，保留全部样本并报告中位数与范围。样本足够时再报告分位数。
4. 避免多个实验争用同一设备。工具或系统限制须写入记录，不能以缺失值冒充 0。
5. 对齐输出采样数、声道和增益后做误差比较。阈值依据参考实现的数值波动和客户要求制定。
6. 只在同一测量协议下比较；报告优化前后性能及质量变化。

## 实验记录模板

复制 [benchmarks/README.md](../benchmarks/README.md) 中的模板填写。原始产物放 `benchmarks/artifacts/<实验编号>/`，摘要放 `benchmarks/<实验编号>.md`；在后验文档中链接实验结论。

当前无可公布的加速比、RTF、峰值内存或质量误差。是否要求实时处理也尚未决定。
