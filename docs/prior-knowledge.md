# 先验知识与待定问题

记录日期：2026-09-10。这里存放要求、经验反馈与假设；经验证的事实转入 [后验知识](posterior-knowledge.md)，并保留证据。模型清单以 [任务](tasks.md) 为准。

## 来源为客户反馈

- 客户指定 1296 处理伴奏，认为其比 MDX23C 更符合速度与效果要求。
- 客户认可 5-HP／6-HP Karaoke 的去和声效果。
- 客户认可 DeEcho-DeReverb 的去混响与 delay／echo 效果。

这些是目标素材上的选型输入，不是跨数据集的性能排名，也不是本项目实测结论。此处 delay 指音频中的延迟效果；若实际需求是轨道时间对齐，需要另行明确。

## 已确定偏好

2026-09-13 用户要求 CLI 与 GUI 暴露当前后端全部可调推理参数，并把当前有性能证据的最佳运行时构建接入前端。文档直接面向原版 UVR 用户，主要突出 CPU VR 的 RTF 约 2 对比 Python 约 6、原生 CLI 核心约 9 MB 和较小的 GUI 体积；数字的配置与产物范围见[性能与体积说明](performance-summary.zh-CN.md)。这组产品定位来自用户要求，归档数字与控制实验的区别由证据文档说明。

同日用户要求恢复原声波图标，加入浅色／深色和主题切换，以及中文、日文、英文支持。Windows 交叉编译问题只作技术讨论：交叉编译本身不决定性能，发布前仍需在 Windows 目标机器核验工具链、CPU 指令、原生依赖和推理结果；当前已验证平台继续为 Linux。

推理优先纯 Rust，GUI 不受此约束。此前讨论中的 Rust + ONNX Runtime 只是候选建议，未获选用，不作为项目默认方案。Tauri、TypeScript 和未来可能的 dsh 集成来自原有 `gui/Agent.md`；首版排除 AI 基础功能。

2026-09-10 用户明确：**运行时不依赖 Python，验证可以使用 Python**。Python／PyTorch 仅在独立 `tools/reference/` 环境核验权重、生成参考数据，不进入 Cargo／pnpm 产品构建、应用启动或音频处理链。普通 Rust 测试直接读取版本库固定样本，无需安装 Python。这取代此前“离线验证工具边界待确认”的假设。

同日用户说明客户没有提供基线，授权项目自行决定。当前参数、运行平台及验收预算见 [工程基线 v1](baseline.md)；下面保留的讨论问题不再构成等待客户答复的前置条件。

## 需要验证的判断

| 假设 | 验证方法 |
| --- | --- |
| 三个网络实现可以覆盖四套指定权重 | 检查实际权重张量、网络变体和完整参数映射，运行参考推理 |
| 共用 DSP 层能覆盖 RoFormer 与 VR | 分别对齐窗函数、padding、重采样器、频带裁剪与重建结果 |
| 先分人声，再去和声，再去混响适合作为处理链 | 同一素材比较去和声与去混响的不同顺序及每一步输出 |
| 纯 Rust 路线能满足性能目标 | 明确硬件与预算后，对关键算子和完整模型测量 |
| 同一权重在不同程序中能得到近似结果 | 固定程序版本和预设，检查分块、后处理、精度与增益差异 |

“用 Rust”本身不是加速证据。框架算子覆盖、内存布局、GPU 内核、分块策略与线程调度都可能决定实际表现。

## 本轮先验草案

算法清单、三个网络、CPU 基线、依赖候选和验证顺序记录在 [Rust 推理先验方案](inference-priors.md)；Candle／Burn／ONNX 路线与专用算子比较见 [框架先验](backend-priors.md)。两份文件保留实验前的假设；已经完成的权重和模块实验记录在后验 K008–K011，不能继续把“未进行实验”当作当前状态。

## 推理方案讨论清单

CPU、FP32、batch=1 作为首轮参考实验配置，性能与误差目标仍待验证。

1. 当前先做 CPU 验证；线程数与内存预算怎样依据实验确定？未来 GPU 不在本轮范围。
2. 后续是否允许调用底层 CPU／GPU 原生计算库？离线验证可用 Python，已确认。
3. 先验证哪一个模型与哪一种数值精度？正确性误差阈值如何确定？
4. 接受固定分块长度还是需要动态长度？参考 UVR 的哪些可选后处理必须兼容？
5. 音频格式、最长输入、内存上限、输出位深与采样率要求是什么？

Burn 与 Candle 曾并列候选，后续模块实验支持用 Burn Flex 实现首个 CPU 版本。当前基础参数与质量性能门限已由工程基线决定；最终分发依赖及加速平台仍需相应验证。
