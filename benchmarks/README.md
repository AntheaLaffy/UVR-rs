# 基准记录

测量规则见 [性能协议](../docs/performance.md)。[CPU 后端探针](backend-probe/README.md) 使用独立 workspace；完整 HP Karaoke 测量直接调用 core 的可选 `burn-cpu` 实现。

已有 [5-HP 卷积与上采样](2026-09-10-vr-cpu-blocks.md)、[DeEcho LSTM 与 RoFormer 模块](2026-09-10-sequence-cpu-blocks.md)、[5-HP／6-HP 完整掩码网络](2026-09-10-hp-cpu-network.md)、[DeEcho 完整掩码网络](2026-09-10-deecho-cpu-network.md) 的数值和性能记录，以及 [VR 完整 PCM 音频](2026-09-10-vr-cpu-audio.md) 的正确性对照。[VR CPU 优化实验](2026-09-11-vr-cpu-optimization.md) 新增真实片段的五次热运行 PCM 对照；文件 I/O、整曲、1296 和完整处理链性能仍待验收。

[1296 CPU 优化实验](2026-09-11-roformer-cpu-optimization.md) 补充完整窗采样、保持计算顺序的逐元素优化和成熟后端的模块对照。模块收益与完整波形验证分别记录，不能代替四套模型及完整处理链的性能完成证明。

[1296 OpenVINO 完整后端实验](2026-09-11-roformer-openvino.md) 包含 CPU／GPU 单窗 PCM 重复测量、Rust 原始 checkpoint 构图、可选 core／CLI 文件入口与取消验证，以及安装 Intel 核显运行时后的编译／队列故障对照。缺少依赖时安装后继续验证；内存按实际可用余量以速度优先，失败与未完成范围保留原始记录。

## 实验模板

- 实验编号／日期：
- 问题与比较对象：
- 对应的性能文档候选方向、待验证假设、唯一改变的变量及采用条件：
- Git 提交／是否有未提交变更：
- 构建命令、工具链、特性、锁文件校验和：
- 编译器实验组别、Rust／LLVM 与链接器版本、target triple／CPU、实际 rustc 参数、LTO／codegen-units／panic；编译链接时间、构建峰值内存和二进制大小／摘要：
- PGO 采集与独立评估素材清单、各模型／shape 的采集比例、合并工具版本、profile 摘要及缺失／不匹配诊断（不适用时注明）：
- 系统、CPU、GPU、驱动、内存、电源模式、线程数：
- 实际 ISA／执行内核、各层线程池预算、核心绑定、batch 与算子内部 tile：
- HPC 并行层次与分区方式、并发窗口数、任务粒度、每个线程池的配置／实际活跃线程、总计算线程预算及 SMT／混合核心拓扑：
- 流水线阶段与资源分工、队列／乱序缓冲容量、所有权和复用时机；窗口依赖、固定归并顺序与状态隔离（不适用时注明）：
- 图编译器／执行提供程序版本、优化图／计划、实际融合与布局传播、内核选择／调优记录；优化诊断、汇编及硬件计数器证据：
- 模型、权重与配置校验和：
- 参考实现版本：
- 输入清单与校验和：
- 推理参数、输出格式和数值精度：
- 执行命令及计时边界：
- 预热次数、测量次数、全部原始样本：
- 冷启动／热运行耗时、RTF、分阶段耗时：
- 固定工作量的 T1／Tp、强扩展加速比与效率、相对 2 线程产品基线的收益；user／sys 时间、调度／同步／排队耗时和首个结果延迟：
- 模型准备／权重打包／JIT 时间、缓存生命周期；copy／reorder 字节数与分配次数（注明测量方式）：
- 工作集、tile／步长、缓存行争用与带宽证据；每 worker scratch、在途激活／结果及 padding 内存；设备传输／同步／启动成本（不适用时注明）：
- 峰值 RSS／显存及测量工具：
- 输出长度、增益、误差指标与听评：
- 每窗及取消延迟、尾批／短输入／重试检查；并发时的乱序完成、满队列和错误传播，确认无丢窗、无重复尾窗且不发布未完成音频；各项质量门限通过情况和失败产物：
- 原始产物位置：
- 结论、适用范围与限制：
- 采用／拒绝／待验证状态、单因素与组合收益、复测环境漂移及回退构建：
